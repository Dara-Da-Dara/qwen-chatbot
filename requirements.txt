fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27

# --- needed only for the local (transformers) backend ---
# If you use CHAT_BACKEND=api, you can delete the three lines below
# to get a much smaller, faster deployment.
torch>=2.2
transformers>=4.51
accelerate>=0.30
